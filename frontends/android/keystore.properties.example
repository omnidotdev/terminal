# Copy to keystore.properties and fill in values
# Generate keystore: keytool -genkey -v -keystore omni-terminal.jks -keyalg RSA -keysize 2048 -validity 10000 -alias omni-terminal
storeFile=omni-terminal.jks
storePassword=
keyAlias=omni-terminal
keyPassword=
